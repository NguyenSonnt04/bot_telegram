[project]
name = "tele-shop-bot"
version = "0.1.0"
description = "Telegram webhook gateway for the Telegram Digital Shop platform."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bot_service"]
