[project]
name = "tele-shop-api"
version = "0.1.0"
description = "Business API for the Telegram Digital Shop platform."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api_service"]
